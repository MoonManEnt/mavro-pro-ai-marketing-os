# Deployment Environment Configuration
# This file ensures proper production dependency bundling

# Include development dependencies in production build to fix multer bundling
NODE_ENV=production
NPM_CONFIG_PRODUCTION=false

# Ensure proper package bundling for deployment
NODE_OPTIONS="--experimental-modules --enable-source-maps"